from typing import List
class LocationClass:
    def __init__(self, name, desc, items, timesVisited, places):
        self.name = name 
        self.desc = desc
        self.items = items
        self.timesVisited = timesVisited
        self.places = places